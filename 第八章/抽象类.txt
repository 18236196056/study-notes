【abstract】

1:不能直接由抽象类创建对象，但是可以通过抽象类派生出新的子类，在由其子类来创建对象(也就是不能用new运算符来创建实例对象的类，它可以作为父类被它的所有子类所共享)
2：对抽象方法只需生命而不需实现，即用";"结尾，而不是用"{}"。当一个方法为抽象方法时，意味着这个方法必须被子类的方法所覆盖，否则子类依然为abstrsct的。在抽象方法的声明中，修饰符final、private、native、static和abxtract不能同时使用(抽象类的子类必须实现父类中的所有抽象方法，或者自己也被声明为抽象的)。